.btn-toolbar
  =link_to edit_linen_path(@linen), class: 'btn btn-primary' do
    =fa_icon 'edit'
    Редактировать
  -if @current_user.try(:is_admin?)
    =link_to @linen, data: {method: :delete, confirm: t('helpers.sure')}, class: 'btn btn-danger' do
      =fa_icon 'trash'
      Удалить
