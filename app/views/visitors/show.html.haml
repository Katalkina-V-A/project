.page-header
  %h1
    =@visitor.person.lastname
    =@visitor.person.firstname
    =@visitor.person.secondname
-if @current_user.try(:is_admin?) || @current_user.try(:is_security?)
  =render 'menu'

.row
  .col-md-4
    =@visitor.person.sex
