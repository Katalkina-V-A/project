.page-header
  %h1 Заявки
-if @current_user.try(:is_admin?) || @current_user.try(:is_tenant?)
  .btn-toolbar
    =link_to new_request_path, class: 'btn btn-primary' do
      =fa_icon 'plus'
      Добавить
-if @requests.blank?
  .alert.alert-info Заявок не найдено
-else
  .list-group
    -@requests.each do |request|
      =link_to request, class: 'list-group-item' do
        =request.employee.person.lastname
        .text-muted
          =request.content
  =paginate @requests
