.btn-toolbar
  =link_to edit_request_path(@request), class: 'btn btn-primary' do
    =fa_icon 'edit'
    Ответить
  -if @current_user.try(:is_admin?)
    =link_to @request, data: {method: :delete, confirm: t('helpers.sure')}, class: 'btn btn-danger' do
      =fa_icon 'trash'
      Удалить
