.page-header
  %h1 Жильцы
.btn-toolbar
  =link_to new_tenant_path, class: 'btn btn-primary' do
    =fa_icon 'plus'
    Добавить
.list-group
  -@tenants.each do |tenant|
    =link_to tenant, class: 'list-group-item' do
      .row
        .col-md-8
          =tenant.arrivaldate
          .text-muted
            =tenant.checkoutdate
=paginate @tenants
