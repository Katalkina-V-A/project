= form_for @person, data: {url: stt_people_path()} do |f|
  = error_messages_for @person
  =hidden_field_tag :formname, :edit
  .row
    .col-md-12
      .form-group
        = label_tag('select_type') do
          Выберите тип жителя
        =select_tenant_type()
#tenant_type_fields
