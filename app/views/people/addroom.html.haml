.page-header
  %h1 Заселение в квартиру
= form_for @person do |s|
  =error_messages_for(@person)
  =s.fields_for :student do |g|
    =g.fields_for :tenant do |f|
      = f.fields_for :historymoves do |t|
        =render 'historymove_fields', :f => t
      .links
        =link_to_add_association "Добавить", f, :historymoves, class: 'btn btn-success'
  .form-actions
    = s.submit nil, class: 'btn btn-primary'
