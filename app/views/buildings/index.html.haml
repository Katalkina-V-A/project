.page-header
  %h1 Корпус
-if @current_user.try(:is_admin?)
  .btn-toolbar
    =link_to new_building_path, class: 'btn btn-primary' do
      =fa_icon 'plus'
      Добавить
-if @buildings.blank?
  .alert.alert-info Корпусов не найдено
-else
  .list-group
    -@buildings.each do |building|
      =link_to building, class: 'list-group-item' do
        .row
          .col-md-8
            =building.name
            .text-muted
              =building.address
          .col-md-4
            .progress.progress-striped
              .progress-bar{:style => "width: 20%;"}
                %span.sr-only Доступно: 20/100
  =paginate @buildings
